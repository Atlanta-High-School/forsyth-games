// Map of game URLs to their local paths in the public directory
// Since all games are now served from the Celestrium GitHub repository,
// this mapping is no longer needed as the proxy system handles external URLs
const gamePaths = {};

export default gamePaths;
